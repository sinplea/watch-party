import RoomCode from '../src/components/RoomControls/RoomCode.svelte'

describe('RoomCode Component', () => {
    it('should warn a user when trying to send a message with the incorrect format', () => {
        expect(true).toBe(true);
    });
})