import TopBar from '../src/components/TopBar/TopBar.svelte';

describe('TopBar Aggregate Component', () => {
    it('should display a preview of results', () => {
        expect(true).toBe(true);
    })
})